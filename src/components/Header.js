import React from 'react';
import { Link } from 'react-router-dom';
import styled from 'styled-components';

// This component is no longer used as the header has been moved to Layout.js
// Keeping this file as a placeholder in case it needs to be restored later

const Header = () => {
  return null;
};

export default Header;